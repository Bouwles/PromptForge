# Commit Strategy

PromptForge improvements are easiest to review when each commit has one reason to exist.

Examples:

- one image asset;
- one README change;
- one UI behavior improvement;
- one prompt-engine directive;
- one scanner rule;
- one test file.

Small commits make the showcase history easier to skim on GitHub.
