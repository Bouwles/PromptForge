# CI Notes

The CI workflow installs dependencies, runs the Node test suite, and builds the web bundle on every push and pull request.

This catches prompt-engine drift, scanner regressions, storage issues, and browser build failures before the app is presented as portfolio work.
