# Demo Data

Use this rough request when showing PromptForge:

```text
the battle pass page looks too flat and the rewards are confusing, make it premium and fullscreen, keep firebase saves and inventory working, add tests and dont rewrite the whole game
```

Use this project memory:

```text
Firebase saves player inventory, unlocked cosmetics, and progression. The shop and battle pass share the same cosmetic ownership rules. Existing mobile layout must keep working.
```

This creates a strong before-and-after because the input is messy but the output can become a precise implementation handoff.
