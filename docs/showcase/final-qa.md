# Final QA

Before sharing PromptForge publicly:

- Confirm GitHub shows the README media.
- Confirm CI is green on `main`.
- Run the local app and create a one-off prompt.
- Open a project, scan files, pin context, generate, and save.
- Search History and duplicate a saved prompt.
- Verify the repo has no local-only commits.
