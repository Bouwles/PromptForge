# Portfolio Positioning

Lead with the engineering story:

PromptForge is not a wrapper around a model. It is a local developer tool with a desktop runtime, browser fallback, scanner, prompt engine, storage layer, streaming generation, refinement actions, and tests.

The strongest positioning line is:

> A local-first prompt engineering workbench that turns messy coding requests and project context into disciplined handoffs for coding agents.
