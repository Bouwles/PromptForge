# Web Permissions

The browser build needs explicit permission for local folders.

When a folder handle expires, reconnect the folder from the project workspace. PromptForge should treat this as a recoverable permission state, not data loss.

Browser storage is local to the site origin. Clearing site data removes saved web-mode prompts and projects.
