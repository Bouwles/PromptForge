# Web Demo Guide

The web build uses the same React app as the desktop build and swaps the runtime API for browser-friendly storage and scanning.

```bash
npm run build:web
```

The strongest public demo path is local Ollama from the visitor's machine. If you connect a hosted model proxy, lock origins, restrict models, and document the cost controls.
