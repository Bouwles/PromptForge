# Offline State Example

```text
Improve the offline state when Ollama is down. Explain the exact recovery steps, keep Settings reachable, and make sure no generate button looks usable without a model.
```

Best prompt type: `Fix Bug`

Best target agent: `Claude Code`
