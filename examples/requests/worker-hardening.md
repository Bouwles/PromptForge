# Worker Hardening Example

```text
Harden the Cloudflare Worker proxy. Validate method and JSON payloads, lock allowed origins, limit exposed models, and return user-safe errors without leaking secrets.
```

Best prompt type: `Refactor Code`

Best target agent: `Claude Code`
