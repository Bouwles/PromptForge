# Performance Example

```text
The project dashboard stutters when there are many cards and history rows. Profile rendering, reduce unnecessary rerenders, keep search behavior identical, and add a measurable before/after note.
```

Best prompt type: `Improve Performance`

Best target agent: `Claude Code`
