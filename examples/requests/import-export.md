# Import Export Example

```text
Add JSON export/import for saved prompts. Preserve favorites, project links when possible, and validate imported rows so corrupted files cannot break history.
```

Best prompt type: `Build Feature`

Best target agent: `Claude Code`
