# Prompt Actions Example

```text
Add a prompt action that turns a generated prompt into a strict implementation checklist. Preserve all existing actions and add tests that reject unknown actions.
```

Best prompt type: `Add System`

Best target agent: `Cursor`
