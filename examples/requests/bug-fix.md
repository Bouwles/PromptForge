# Bug Fix Example

```text
Users sometimes lose selected cosmetics after refreshing the app. Find the save/load issue, preserve existing inventory data, and add regression tests that prove owned cosmetics persist across reload.
```

Best prompt type: `Fix Bug`

Best target agent: `Cursor`
