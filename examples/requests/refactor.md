# Refactor Example

```text
Separate the prompt generation logic from the UI component without changing generated output. Keep all actions, saved prompts, and tests working. Add a parity test if behavior is shared between desktop and web.
```

Best prompt type: `Refactor Code`

Best target agent: `Windsurf`
