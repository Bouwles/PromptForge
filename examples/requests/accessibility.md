# Accessibility Example

```text
Audit the editor flow for keyboard and screen reader use. Fix missing labels, visible focus, toggle semantics, and disabled states without changing the app layout.
```

Best prompt type: `Debug Error`

Best target agent: `Claude Code`
