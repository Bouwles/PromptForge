# Test Suite Example

```text
Add focused tests for project-aware prompt context. Cover memory, important systems, restrictions, pinned files, truncated files, and the no-project fallback.
```

Best prompt type: `Build Feature`

Best target agent: `Cursor`
